#include "MErr.r"

resource 'MErr' (15840, "stale tnt res handle") {
	{	/* array Strs: 1 elements */
		/* [1] */
		"Corrupt or stale TNT resource handle detected."
	}
};

resource 'MErr' (15841, "resource already exists") {
	{	
		"'ÆResT' resource ÆResN already exists.",
		"Resource already exists."
	}
};


resource 'MErr' (15001, "No browser for resource type") {
	{	/* array Strs: 1 elements */
		/* [1] */
		"There is no browser available to view th"
		"at type."
	}
};

resource 'MErr' (15000, "No viewer for resource") {
	{	/* array Strs: 1 elements */
		/* [1] */
		"There is no viewer available to view tha"
		"t resource."
	}
};

resource 'MErr' (-5000, "AppleTalk reports access denied.") {
	{	/* array Strs: 1 elements */
		/* [1] */
		"AppleTalk reports access denied."
	}
};

resource 'MErr' (-1, "Misc err") {
	{	/* array Strs: 1 elements */
		/* [1] */
		"A miscellaneous error occurred."
	}
};

resource 'MErr' (27728, "Nil pointer") {
	{	/* array Strs: 1 elements */
		/* [1] */
		"Nil pointer detected."
	}
};

resource 'MErr' (-623, "Mem not locked") {
	{	/* array Strs: 1 elements */
		/* [1] */
		"Illegal operation on unlocked memory blo"
		"ck."
	}
};

resource 'MErr' (-111, "Scrap manager which zone failed") {
	{	/* array Strs: 1 elements */
		/* [1] */
		"Scrap manager reports which zone failed."
		"\n(This error is also sometimes generated"
		" if you start adding handles which are a"
		"lready resources to a resource file.)"
	}
};

resource 'MErr' (-1861, "bad image rgn") {
	{	/* array Strs: 1 elements */
		/* [1] */
		"Drag manager reports bad image region."
	}
};

resource 'MErr' (-1860, "bad drag rgn") {
	{	/* array Strs: 1 elements */
		/* [1] */
		"Drag manager reports bad drag region."
	}
};

resource 'MErr' (-1859, "no good disp") {
	{	/* array Strs: 1 elements */
		/* [1] */
		"Drag manager reports there are not displ"
		"ays suitable for translucency effects."
	}
};

resource 'MErr' (-1858, "ppc only") {
	{	/* array Strs: 1 elements */
		/* [1] */
		"Drag manager reports that call is for Po"
		"werMacs only."
	}
};

resource 'MErr' (-1857, "drag not accepted") {
	{	/* array Strs: 1 elements */
		/* [1] */
		"Drag manager reports the drag was not ac"
		"cepted by the recipient."
	}
};

resource 'MErr' (-1856, "handler not found") {
	{	/* array Strs: 1 elements */
		/* [1] */
		"Drag manager reports handler not found."
	}
};

resource 'MErr' (-1855, "dup handler") {
	{	/* array Strs: 1 elements */
		/* [1] */
		"Attempt to add duplicate drag handler."
	}
};

resource 'MErr' (-1854, "can't get flavour err") {
	{	/* array Strs: 1 elements */
		/* [1] */
		"Drag manager can't get flavour data."
	}
};

resource 'MErr' (-1853, "dupflavourerr") {
	{	/* array Strs: 1 elements */
		/* [1] */
		"Drag manager reports duplicate flavour e"
		"rror."
	}
};

resource 'MErr' (-1852, "bad flavour") {
	{	/* array Strs: 1 elements */
		/* [1] */
		"Drag manager reports bad drag flavour."
	}
};

resource 'MErr' (-1851, "bad drag item") {
	{	/* array Strs: 1 elements */
		/* [1] */
		"Drag manager reports bad drag item refer"
		"ence."
	}
};

resource 'MErr' (-1850, "bad drag ref") {
	{	/* array Strs: 1 elements */
		/* [1] */
		"Drag manager reports bad drag reference."
	}
};

resource 'MErr' (-45, "The file is locked.") {
	{	/* array Strs: 1 elements */
		/* [1] */
		"The file is locked."
	}
};

resource 'MErr' (-44, "Disk write protected") {
	{	/* array Strs: 1 elements */
		/* [1] */
		"The disk is write protected."
	}
};

resource 'MErr' (-39, "EOF Err") {
	{	/* array Strs: 1 elements */
		/* [1] */
		"Unexpected end of file."
	}
};

resource 'MErr' (-34, "Disk full") {
	{	/* array Strs: 1 elements */
		/* [1] */
		"The disk is full."
	}
};

resource 'MErr' (-33, "dirfull") {
	{	/* array Strs: 1 elements */
		/* [1] */
		"That directory can hold no more files."
	}
};

resource 'MErr' (-124, "volGone") {
	{	/* array Strs: 1 elements */
		/* [1] */
		"The server has been disconnected."
	}
};

resource 'MErr' (-61, "WrPermErr") {
	{	/* array Strs: 1 elements */
		/* [1] */
		"Write permissions error."
	}
};

resource 'MErr' (-54, "file perm error") {
	{	/* array Strs: 1 elements */
		/* [1] */
		"The file cannot be opened with those per"
		"mission levels."
	}
};

resource 'MErr' (-51, "refnum err") {
	{	/* array Strs: 1 elements */
		/* [1] */
		"Invalid file refnum."
	}
};

resource 'MErr' (-49, "WriteOPErr") {
	{	/* array Strs: 1 elements */
		/* [1] */
		"File already open with write permission."
	}
};

resource 'MErr' (-48, "dupfilename") {
	{	/* array Strs: 1 elements */
		/* [1] */
		"A file with that name already exists."
	}
};

resource 'MErr' (-47, "filebusy") {
	{	/* array Strs: 1 elements */
		/* [1] */
		"The file is in use."
	}
};

resource 'MErr' (-46, "vol locked err") {
	{	/* array Strs: 1 elements */
		/* [1] */
		"The volume is locked and so cannot be wr"
		"itten to."
	}
};

resource 'MErr' (-112, "memPurErr") {
	{	/* array Strs: 1 elements */
		/* [1] */
		"Illegal attempt to purge a locked or non"
		"-purgeable memory block."
	}
};

resource 'MErr' (-109, "Nil handle") {
	{	/* array Strs: 1 elements */
		/* [1] */
		"Attempt to operate on a nil handle."
	}
};

resource 'MErr' (-147, "Rgn overflow") {
	{	/* array Strs: 1 elements */
		/* [1] */
		"Region overflow due to insufficient memo"
		"ry."
	}
};

resource 'MErr' (-145, "NoMemForPictPlayback") {
	{	/* array Strs: 1 elements */
		/* [1] */
		"There is not enough memory to playback t"
		"he picture."
	}
};

resource 'MErr' (-185, "BadExtFormat") {
	{	/* array Strs: 1 elements */
		/* [1] */
		"Resource has bad extended format."
	}
};

resource 'MErr' (-186, "Can't decomp resource") {
	{	/* array Strs: 1 elements */
		/* [1] */
		"Unable to decompress a compressed resour"
		"ce."
	}
};

resource 'MErr' (-199, "Map read err") {
	{	/* array Strs: 1 elements */
		/* [1] */
		"The resource map of the file is inconsis"
		"tent with the operation."
	}
};

resource 'MErr' (-198, "Res attr err") {
	{	/* array Strs: 1 elements */
		/* [1] */
		"The resource attributes are inconsistent"
		" with the operation."
	}
};

resource 'MErr' (-196, "rmv res failed") {
	{	/* array Strs: 1 elements */
		/* [1] */
		"Removal of resource failed."
	}
};

resource 'MErr' (-194, "addresfailed") {
	{	/* array Strs: 1 elements */
		/* [1] */
		"Addition of resource failed."
	}
};

resource 'MErr' (-193, "Res f not found") {
	{	/* array Strs: 1 elements */
		/* [1] */
		"The resource file is no longer open."
	}
};

resource 'MErr' (-192, "Res not found") {
	{	
		"'ÆResT' resource ÆResN is missing.",
		"The resource is missing."
	}
};

resource 'MErr' (-188, "Res In Mem") {
	{	/* array Strs: 1 elements */
		/* [1] */
		"Resource already in memory."
	}
};

resource 'MErr' (-117, "Mem locked") {
	{	/* array Strs: 1 elements */
		/* [1] */
		"Attempt to moved a locked memory handle."
	}
};

resource 'MErr' (-128, "User cancelled") {
	{	/* array Strs: 1 elements */
		/* [1] */
		"The operation was cancelled at your requ"
		"est."
	}
};

resource 'MErr' (-2809, "Frag mem err") {
	{	/* array Strs: 1 elements */
		/* [1] */
		"Out of memory."
	}
};

resource 'MErr' (-2825, "bad file size") {
	{	/* array Strs: 1 elements */
		/* [1] */
		"The code fragment is too large to be fil"
		"e mapped."
	}
};

resource 'MErr' (-2823, "bad architecture") {
	{	/* array Strs: 1 elements */
		/* [1] */
		"The code fragment has a bad architecture"
		"."
	}
};

resource 'MErr' (-2820, "frag corrupt") {
	{	/* array Strs: 1 elements */
		/* [1] */
		"The code fragment is corrupt."
	}
};

resource 'MErr' (-2817, "lib conn err") {
	{	/* array Strs: 1 elements */
		/* [1] */
		"A problem was encountered connecting to "
		"the library."
	}
};

resource 'MErr' (-2814, "import too new") {
	{	/* array Strs: 1 elements */
		/* [1] */
		"A library related to the code fragment w"
		"as too new to be used so the fragment co"
		"uld not initialize."
	}
};

resource 'MErr' (-2813, "import too old") {
	{	/* array Strs: 1 elements */
		/* [1] */
		"A library related to the code fragment w"
		"as too old to be used so the fragment co"
		"uld not initialize."
	}
};

resource 'MErr' (-2807, "Unresolved sym") {
	{	/* array Strs: 1 elements */
		/* [1] */
		"The code fragment contains unacceptable "
		"unresolved symbols. The first missing sy"
		"mbol is \"^1\"."
	}
};

resource 'MErr' (-2806, "Bad frag format") {
	{	/* array Strs: 1 elements */
		/* [1] */
		"The code fragment container version is u"
		"nknown."
	}
};

resource 'MErr' (-2804, "no lib err") {
	{	/* array Strs: 1 elements */
		/* [1] */
		"The library \"^1\" could not be found."
	}
};

resource 'MErr' (-2802, "No symbol err") {
	{	/* array Strs: 1 elements */
		/* [1] */
		"The specified CFM symbol could not be fo"
		"und."
	}
};

resource 'MErr' (-120, "Dir not found") {
	{	/* array Strs: 1 elements */
		/* [1] */
		"The directory is missing."
	}
};

resource 'MErr' (-43, "File not found") {
	{	/* array Strs: 1 elements */
		/* [1] */
		"The file is missing."
	}
};

resource 'MErr' (-35, "Vol not found") {
	{	/* array Strs: 1 elements */
		/* [1] */
		"The specified volume is not currently pr"
		"esent."
	}
};

resource 'MErr' (-50, "Param err") {
	{	/* array Strs: 1 elements */
		/* [1] */
		"There was an error in the parameters use"
		"d to initiate an operation. Unfortunatel"
		"y, this can happen for a number of diffe"
		"rent reasons and can only be determined "
		"on a case by case basis."
	}
};

resource 'MErr' (-108, "Mem full") {
	{	/* array Strs: 1 elements */
		/* [1] */
		"There was insufficient memory to complet"
		"e the operation."
	}
};

resource 'MErr' (-2, "Assertion failed") {
	{	/* array Strs: 1 elements */
		/* [1] */
		"Assertion failed."
	}
};

resource 'MErr' (-3, "Unsupported data format") {
	{	/* array Strs: 1 elements */
		/* [1] */
		"Unsupported data format."
	}
};

resource 'MErr' (15800, "Tag not found") {
	{	/* array Strs: 1 elements */
		/* [1] */
		"Tag missing in tagged data store."
	}
};

resource 'MErr' (15815, "Nodes in seperate graphs") {
	{	/* array Strs: 1 elements */
		/* [1] */
		"Cannot link graph nodes as they're in se"
		"perate graphs."
	}
};

resource 'MErr' (15816, "null graph") {
	{	/* array Strs: 1 elements */
		/* [1] */
		"Attempt to use null graph."
	}
};

resource 'MErr' (15817, "index out of boudns") {
	{	/* array Strs: 1 elements */
		/* [1] */
		"Index out of bounds."
	}
};

resource 'MErr' (15820, "bad persistor id") {
	{	/* array Strs: 1 elements */
		/* [1] */
		"Unknown reanimation id."
	}
};

resource 'MErr' (-19, "readErr") {
	{	/* array Strs: 1 elements */
		/* [1] */
		"Read error."
	}
};

resource 'MErr' (-20, "Write error") {
	{	/* array Strs: 1 elements */
		/* [1] */
		"Write error."
	}
};

resource 'MErr' (15821, "dupe persistor id") {
	{	/* array Strs: 1 elements */
		/* [1] */
		"Duplicate persistor class reanimator reg"
		"istered."
	}
};

resource 'MErr' (-1700, "coercion fail") {
	{	/* array Strs: 1 elements */
		/* [1] */
		"Could not make some AppleEvent data into"
		" the correct type."
	}
};

resource 'MErr' (-1708, "ae not handled") {
	{	/* array Strs: 1 elements */
		/* [1] */
		"The AppleEvent was not handled."
	}
};

resource 'MErr' (-1711, "ae wait cancelled") {
	{	/* array Strs: 1 elements */
		/* [1] */
		"The wait for an AppleEvent response was "
		"cancelled."
	}
};

resource 'MErr' (-1712, "ae time out") {
	{	/* array Strs: 1 elements */
		/* [1] */
		"The time limit for a response to the App"
		"leEvent has expired."
	}
};

resource 'MErr' (15825, "TNT Music engine: no music loaded") {
	{	/* array Strs: 1 elements */
		/* [1] */
		"There is no loaded music!"
	}
};

resource 'MErr' (-4960, "code foundation err") {
	{	/* array Strs: 1 elements */
		/* [1] */
		"Macintosh core foundation miscellaneous "
		"error.\nNo I don't know what this means e"
		"ither."
	}
};

resource 'MErr' (-4989, "dupe scrap flav") {
	{	/* array Strs: 1 elements */
		/* [1] */
		"Duplicate scrap flavour detected."
	}
};

resource 'MErr' (-4990, "bad scrap ref") {
	{	/* array Strs: 1 elements */
		/* [1] */
		"Invalid scrap reference."
	}
};

resource 'MErr' (-4991, "bad process state") {
	{	/* array Strs: 1 elements */
		/* [1] */
		"Scrap manager reports that the process s"
		"tate is incorrect."
	}
};

resource 'MErr' (-4993, "no scrap promise keeper") {
	{	/* array Strs: 1 elements */
		/* [1] */
		"Some data on the scrap was promised, but"
		" the providing application isn't providi"
		"ng. Most probably a bug in the applicati"
		"on you copied the scrap from."
	}
};

resource 'MErr' (-4994, "nil scrap flavour") {
	{	/* array Strs: 1 elements */
		/* [1] */
		"Some data on the scrap is missing."
	}
};

resource 'MErr' (-100, "need clear scrap") {
	{	/* array Strs: 1 elements */
		/* [1] */
		"The scrap needs to be reset."
	}
};

resource 'MErr' (-102, "scrap flavour not found") {
	{	/* array Strs: 1 elements */
		/* [1] */
		"There is no appropriate data on the scra"
		"p."
	}
};

resource 'MErr' (-30879, "bad print session") {
	{	/* array Strs: 1 elements */
		/* [1] */
		"Invalid printing session."
	}
};

resource 'MErr' (-30875, "bad print settings") {
	{	/* array Strs: 1 elements */
		/* [1] */
		"Invalid printing settings."
	}
};

resource 'MErr' (-30876, "bad print format") {
	{	/* array Strs: 1 elements */
		/* [1] */
		"Invalid page format."
	}
};

resource 'MErr' (-30877, "print bad range") {
	{	/* array Strs: 1 elements */
		/* [1] */
		"Invalid range."
	}
};

resource 'MErr' (-30880, "bad printer") {
	{	/* array Strs: 1 elements */
		/* [1] */
		"Invalid printer."
	}
};

resource 'MErr' (-30881, "printer in use") {
	{	/* array Strs: 1 elements */
		/* [1] */
		"The printer is busy."
	}
};

resource 'MErr' (15002, "res not supported") {
	{	/* array Strs: 1 elements */
		/* [1] */
		"This particular resource is not supporte"
		"d by this service."
	}
};

resource 'MErr' (15003, "cant find block proc") {
	{	/* array Strs: 1 elements */
		/* [1] */
		"Cannot find the block callback function "
		"required by this service. It is likely t"
		"hat the block has become corrupted and n"
		"eeds reinstalling."
	}
};

resource 'MErr' (-5699, "navmissingkindstring") {
	{	/* array Strs: 1 elements */
		/* [1] */
		"Cannot find the document kind strings.\nT"
		"his may mean you need to rebuild your de"
		"sktop. To do this, restart your Mac and "
		"hold down cmd-option on your keyboard."
	}
};

resource 'MErr' (-4, "STL exception caught") {
	{	/* array Strs: 1 elements */
		/* [1] */
		"An error has occurred whilst accessing t"
		"he STL template library."
	}
};

